import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jykra.dwarepe',
  appName: 'doorstep',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
